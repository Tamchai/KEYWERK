export const panel = { background: "var(--surface)", border: "1px solid var(--line)", borderRadius: 12, padding: 20 } as const;
export const input = { width: "100%", padding: "11px 12px", background: "var(--bg)", color: "var(--text)", border: "1px solid var(--line)", borderRadius: 8 } as const;
export const button = { padding: "10px 16px", background: "var(--accent)", color: "var(--bg)", border: 0, borderRadius: 8, fontWeight: 800, cursor: "pointer" } as const;
